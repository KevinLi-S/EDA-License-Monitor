## 2026-03-12 EDA License Hub 联调进展

- 用户要求继续设计并联调 `eda-license-hub`，最新交付包为 `eda-license-hub/build/eda-license-hub-v1.3.2.tar.gz`。
- 用户提供了模拟生产环境测试机：`192.168.110.128`。
- 测试机可 SSH 登录，凭据由用户提供；目标部署目录为 `/eda/tools/eda-license-hub`。

### 页面与方案进展
- 已梳理并落地页面升级方案文档：`eda-license-hub/DESIGN_PROD_UI_PLAN.md`。
- 已完成前端一轮生产化页面改造：
  - Dashboard
  - Servers
  - License Keys
  - Alerts
  - Logs
  - Upload
- 本地前端可运行，曾提供本地地址 `http://localhost:5173/` 用于展示。

### 远端环境探查结果
- 目标机系统：CentOS 7。
- 系统 Python：3.6.8。
- 无 Node、无 Nginx。
- `firewalld` 未运行。
- License 目录确认：`/eda/env/license`。
- 日志目录确认：`/eda/env/license/log`。
- 目标机正在运行 Synopsys / Cadence / Mentor / Ansys 的 lmgrd/vendor daemon。

### 部署策略演进
- 初始尝试做“便携离线部署（方案 B）”，目标是不依赖系统环境、解压即用。
- 已在远端落地 Miniconda 便携运行时到：`/eda/tools/eda-license-hub/runtime/miniconda`。
- 已完成一套 Python 3.8 对齐的离线依赖安装到：`/eda/tools/eda-license-hub/backend/.venv`。
- 后续与用户对齐后，阶段目标改为：
  1. 先把数据联动和页面展示跑通；
  2. 最后再收敛成离线打包部署方案。

### 已定位的关键问题
- 后端真实启动报错已拿到，不再是纯部署猜测。
- 已确认并修复两处 Python 3.8 兼容问题：
  1. `backend/app/models/entities.py` 中 `datetime | None` / `int | None` 语法不兼容 Python 3.8；已改为 `Optional[...]`。
  2. `backend/app/schemas.py` 中 `list[...]` 不兼容当前环境；已改为 `List[...]`。
- 这些修复已经同步到远端。

### 当前状态（写入时）
- 远端后端依赖已装好，`.venv` 可导入 `fastapi/sqlalchemy/pydantic/uvicorn`。
- 后端启动问题已经从“环境/依赖问题”收敛到“逐个消除 Python 语法兼容问题”。
- 还未最终确认 `8000` 与 `8080` 已监听；页面和 API 尚未宣布成功可访问。
- 当前最优先目标：
  - 在远端用 `.venv/bin/uvicorn` 跑通 `app.main:app`；
  - 验证 `http://127.0.0.1:8000/api/health`；
  - 再把前端静态页面挂起来展示真实数据。

### 用户偏好 / 沟通要求
- 用户明确要求：如果长时间无消息，需要主动反馈进度，否则会认为助手已停止工作。
- 用户后续要求把当前信息写入 memory，便于恢复上下文。
